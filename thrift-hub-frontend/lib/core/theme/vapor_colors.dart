import 'package:flutter/material.dart';

class VaporColors {
  // Primary brand colors - Changed to thrift/vintage theme
  static const Color primary = Color(0xFF8B4513);        // Saddle Brown
  static const Color secondary = Color(0xFF6B4423);      // Dark Brown  
  static const Color accent = Color(0xFFD2691E);         // Chocolate Orange
  
  // Vapor effects - Changed to vintage theme
  static const Color vapor = Color(0xFFDEB887);          // Burlywood
  static const Color cloud = Color(0xFFF5DEB3);          // Wheat
  static const Color device = Color(0xFF4682B4);         // Steel Blue
  
  // Text colors - Updated for better contrast
  static const Color textPrimary = Color(0xFF2F1B14);    // Dark Brown
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFF9CA3AF);
  
  // Status colors - Keep the same
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);
}
